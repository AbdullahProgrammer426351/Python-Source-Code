def average(a=9, b=1):#default arguments
    print("The average is ", (a+b)/2)

print("=============== Key word arguments ==================")
# if we don't want to follow sequence of parameters, we can use this feature
average(b=9,a=90)